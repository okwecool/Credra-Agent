# Credra Agent 开发日志

本日志记录开发过程中与需求分析、技术设计方案或开发计划存在的出入，以及为适应实际依赖、框架行为、运行环境和验证结果所作的调整。

日志仅记录实施事实和设计偏差，不替代原始需求与开发计划。正式变更文档基线前需经过人工审核。

---

## 2026-08-12 — Day 0 Durable Execution Technical Spike

- **对应阶段：** Day 0 — Checkpoint / Interrupt / Kill Process / Resume。
- **文档原约定：** 使用 `node_a → interrupt() → 退出进程 → Command(resume) → node_b` 的最小流程，验证 SQLite Checkpoint、相同 `thread_id` 跨进程恢复，以及恢复后不重新执行已完成节点。
- **实际情况：** LangGraph 在 Resume 时会从包含 `interrupt()` 的节点开头重新执行。若 `node_a` 自身包含 interrupt，无法清晰区分“中断节点按框架语义重新进入”与“中断前已完成节点被错误重复执行”。
- **修改内容：** Spike 调整为 `prepare → approval(interrupt) → complete` 三节点流程；由 `prepare_runs` 记录 interrupt 前已完成节点的执行次数，并通过独立 Python 子进程分别执行 start、status 和 resume。
- **修改原因：** 在遵守 interrupt 节点无副作用约束的同时，准确验证已完成节点不会在跨进程 Resume 后重新执行。
- **影响范围：** 仅影响 Day 0 Spike 的验证结构，不改变后续业务 Graph、MVP 范围或验收目标。
- **验证结果：** SQLite 成功保存线程 Checkpoint；新进程使用相同 `thread_id` 恢复后任务进入 `COMPLETED`，且 `prepare_runs` 保持为 `1`；跨进程自动化测试共 2 项通过。

---

## 2026-08-12 — Day 1 Core Business Flow

- **对应阶段：** Day 1 — Financial → Risk → Report 最小业务纵向链路。
- **文档原约定：** Risk Agent 和固定模板报告由 LLM 生成表达，并通过 Pydantic Schema 约束结果。
- **实际情况：** 当前尚未配置可供自动化测试使用的模型凭证；若把外部模型调用作为 Day 1 门禁，核心财务公式、Schema 和纵向流程将无法离线稳定回归。
- **修改内容：** 保留外置 Risk/Report Prompt，并先实现确定性的结构化 Risk 基线与固定 Markdown 报告渲染器；财务计算仍全部由 Python Tool 执行。Risk 与 Report 通过独立函数边界提供，后续接入模型适配器时不改变 Financial Tool 和 Pydantic 数据契约。
- **修改原因：** 使 Day 1 纵向链路在无模型凭证情况下可重复测试，同时避免 LLM 参与确定性计算。
- **影响范围：** Day 1 的风险解释和报告文本暂为确定性基线；不影响后续动态路由、Artifact、HITL 或模型接入设计。
- **验证结果：** 两个固定 Case 均完成 Financial → Risk → Report；财务公式、缺失字段、年份顺序和零分母边界测试通过；Risk Pydantic 约束和固定报告章节测试通过；包含 Day 0 回归在内共 14 项测试通过。

---

## 2026-08-12 — 基础模型连接配置

- **对应阶段：** Day 1 — 配置与 Schema。
- **文档原约定：** 模型配置包含 `MODEL_NAME` 和 `MODEL_API_KEY`。
- **实际情况：** 用户需要自行配置兼容 OpenAI 调用方式的 API Base URL、模型名称和 API Key，以支持非默认模型服务端点。
- **修改内容：** 新增 `MODEL_BASE_URL`，与既有 `MODEL_NAME`、`MODEL_API_KEY` 组成模型连接配置；配置 Schema 提供 OpenAI 默认 Base URL，并将 API Key 读取为自动脱敏的 `SecretStr`，同时增加环境变量加载测试。
- **修改原因：** 允许在不修改应用代码的情况下切换兼容 OpenAI API 的基础模型服务。
- **影响范围：** 仅扩展模型连接配置，不改变当前财务计算、风险 Schema 或报告契约。
- **验证结果：** 环境变量中的 Base URL、模型名称和 API Key 均可正确加载，API Key 在配置对象中以 `SecretStr` 脱敏；包含既有回归在内共 15 项测试通过。

---

## 2026-08-12 — Day 2 Artifact 与 Dynamic Routing

- **对应阶段：** Day 2 — Document、Artifact、异常检测与动态路由。
- **文档原约定：** Case B 在 Financial 后进入 Research，再进入 Risk；Day 3 才实现 Research Agent 和 MCP Server。
- **实际情况：** Day 2 必须验收 `Document → Financial → Research → Risk` 路径，但本阶段尚无真实 Research Tool，也不应伪造外部调查事实或 Research Artifact。
- **修改内容：** Day 2 的 `research` 节点仅作为无副作用路径标记，证明条件路由选择成功；节点不生成调查事实，`research_artifact` 保持为空。执行路径从 LangGraph 更新流收集，不写入最小 Graph State。Day 3 将用 MCP-backed Research 实现替换该节点。
- **修改原因：** 保持开发阶段边界清晰，在验证动态执行的同时避免提前实现或伪造 Day 3 能力。
- **影响范围：** Case B 的 Day 2 风险分析仍只使用财务 Artifact；Research 数据及其风险证据将在 Day 3 接入。
- **验证结果：** Case A 路径为 `document → financial → risk` 且无异常标记；Case B 路径为 `document → financial → research → risk`，稳定生成 `REVENUE_CASHFLOW_DIVERGENCE` 与 `DEBT_RATIO_RISING`。两组 Case 均生成 Company、Financial、Risk 三类 v1 Artifact，Graph State 保持 13 个最小字段且不包含大块对象；包含既有回归在内共 25 项测试通过。

---

## 2026-08-12 — Day 3 Research Agent 与 MCP

- **对应阶段：** Day 3 — Research Agent、Research MCP Server、`search_company` 与 `search_industry`。
- **文档原约定：** Research Agent 通过 MCP Client 调用本地 Mock Dataset/Search Adapter，将事实、来源和查询状态写入 Research Artifact。
- **实际情况：** Windows 下若通过文件路径直接执行 `app/mcp/research_server.py`，子进程模块搜索路径只包含脚本目录，无法解析顶层 `app` 包；此外同步 LangGraph 节点需要适配 FastMCP 的异步 Client。
- **修改内容：** 默认 stdio Transport 使用当前解释器执行 `python -m app.mcp.research_server`；同步 Graph 节点通过明确的 `asyncio.run` 边界调用异步 MCP Client，并在检测到已有事件循环时抛出可识别错误。公司与行业两个 Tool 在同一 MCP Session 中顺序调用。测试使用 FastMCP 内存 Transport验证相同协议契约，另提供独立 stdio 子进程验收脚本。
- **修改原因：** 保证项目包导入稳定、Windows 子进程行为可复现，并避免一次调查任务重复建立 MCP Session。
- **影响范围：** Research Agent 的运行边界和测试方式；不改变 Graph State、路由规则或 Artifact 引用结构。异步 Graph 运行方式属于后续扩展项。
- **验证结果：** `search_company` 与 `search_industry` 均通过 MCP 返回带来源标识的结构化事实，未知查询显式返回 `found=false` 和空事实；不可用 Server 被转换为 `ResearchServiceError`。Case A 保持 `document → financial → risk`，Case B 通过独立 stdio MCP Server 完成 `document → financial → research → risk`，生成 `research_result_v1.json`，Risk Artifact 的 evidence 包含 MCP `source_id`；全量 28 项测试及 stdio 子进程验收通过。

---

## 2026-08-13 — Day 4 HITL 与完整跨进程 Resume

- **对应阶段：** Day 4 — Risk 审核、interrupt、HumanDecision、SQLite Checkpoint、跨进程 Resume 与最小 Chainlit UI。
- **文档原约定：** `MEDIUM/HIGH` 风险进入 `WAITING_APPROVAL`；`APPROVE` 继续报告，`RESEARCH` 补充调查；新进程凭相同 `thread_id` 恢复，Research 重新执行时不得覆盖历史 Artifact。
- **实际情况：** Day 2 的无 Checkpoint Runner 在 Graph 增加 Report/Interrupt 后不再适合完整生命周期；同时 `status/resume` 若要求用户重复提供 `case_id`，会增加恢复错误风险。Chainlit CLI 直接加载 `app/chainlit_app.py` 时只使用该脚本目录解析模块，无法导入顶层 `app` 包。
- **修改内容：** 新增 SQLite-backed Task Runtime，由 Checkpoint 的 `channel_values.case_id` 自动重建 Case Graph；提供 `start/status/resume` CLI。Risk 后按风险等级进入动态 interrupt；Research 决策生成 `research_result_vN` 和 `risk_analysis_vN` 后再次审核。低风险路径扩展为 `document → financial → risk → report`。最小 Chainlit UI 使用相同 Runtime，并保留 CLI 作为降级入口；仓库根目录增加轻量 `chainlit_app.py` 启动入口以保持包导入稳定。
- **修改原因：** 确保任务生命周期长于进程和 UI Session，减少 Resume 输入，并让 Artifact 版本与持久化 State 保持一致。
- **影响范围：** Graph 终止条件、Artifact 写入幂等策略、报告人工意见、Task CLI 和 UI；Day 0 Spike 保持独立不变。
- **验证结果：** Case B 在 Risk 后以 `WAITING_APPROVAL` 暂停，独立进程可仅凭相同 `thread_id` 从 SQLite 读取状态并 Resume；`APPROVE` 生成包含人工意见的报告并进入 `COMPLETED`，`RESEARCH` 生成 v2 Research/Risk Artifact 且保留 v1 后再次暂停，第二次批准后完成。Case A 无 interrupt 直接生成报告。同版本不同内容禁止覆盖，下一版本由 State 引用确定。全量 35 项测试通过；手工三进程演示生成 8 条 Checkpoint；Chainlit 根入口成功加载并监听测试端口。

---

## 2026-08-13 — Day 5 Retry、Fault Injection、Trace 与 Regression

- **对应阶段：** Day 5 — Research Tool Retry、可控故障注入、JSONL Trace 和五个固定回归 Case。
- **文档原约定：** `RESEARCH_FAIL_FIRST=1` 时首次调用超时、重试成功；重试耗尽设置 `external_research_incomplete=true`；Trace 覆盖 Node、Tool、Retry、Interrupt 和 Resume；维护五个 Regression Cases。
- **实际情况：** 故障注入需要在跨进程 Graph/Resume 中保持 task/tool 维度的一次性状态，单纯进程内计数会在进程重启后重复注入；两个 Research Tool 也需要独立重试和独立失败结果。
- **修改内容：** 在 `traces/.fault_state/` 使用 `task_id + tool_name` 哈希标记首次尝试，目录由 Git 忽略；Research Agent 分别对两个 Tool 执行 `MAX_RETRY + 1` 次尝试，只捕获临时边界错误。耗尽后生成状态为 `FAILED` 的 Tool 结果及 `INCOMPLETE` Research Artifact，Graph State、Risk 和 Report 均披露证据缺口。JSONL Trace 记录 Task、Node、Tool、Retry、Interrupt、Status 和 Resume 摘要，不记录源文档或凭证。
- **修改原因：** 保证故障演示确定、可重复且跨进程一致，同时允许部分调查成功时保留已取得的事实，不因单个 Tool 失败中断整个任务。
- **影响范围：** Research 结果 Schema、Risk/Report 披露、Graph/Runtime Trace、运行目录和测试 Fixture；不修改 `.env`。
- **验证结果：** `RESEARCH_FAIL_FIRST=1` 的真实 stdio MCP 演示中，两个 Tool 各产生一次 `RETRY` 后成功，Trace 共 17 行且包含 Task、Node、Retry、Tool、Interrupt 和 State 事件。永久失败测试中每个 Tool 按配置耗尽重试，State、Research/Risk Artifact 和报告均披露调查不完整。五个具名 Regression Cases 通过；全量 45 项测试、Ruff 和依赖一致性检查通过。

---

## 2026-08-13 — Day 6 Showcase 与交付收尾

- **对应阶段：** Day 6 — README、架构与时序图、Demo 脚本、环境说明、故障排查、仓库审计和项目展示材料。
- **文档原约定：** 新开发者只根据 README 即可安装和配置环境、运行两个 Case、观察 Retry、在 interrupt 后跨进程 Resume，并找到 Artifact、Trace 和最终报告；同时清理死代码、重复配置、过期命名和敏感运行数据。
- **实际情况：** 正式持久化入口为 `app.task_cli`，而早期 `app.main` 仍是有价值的无 Checkpoint 离线预览；MCP Server 在风险路径中由 Client 自动启动，不需要用户预先维护服务进程。Windows PowerShell 5.1 对外部命令的非零退出码不会仅凭 `$ErrorActionPreference` 自动失败；从已激活 Conda 环境再次启动 `powershell -File` 时，`CONDA_PREFIX` 可能被重置为 base，且无法直接解析目标环境的 `ruff.exe`。此外，用户级 `pytest-of-<user>` 临时目录或仓库 `.pytest_cache` 的 ACL 异常会让 `tmp_path` Fixture 产生 `WinError 5`。
- **修改内容：** 新增项目 README、Mermaid 架构/MCP/Resume 图、四类演示说明、可执行 `scripts/demo.ps1`、统一门禁 `scripts/verify.ps1` 及项目展示与面试说明；验证和 Demo 脚本优先结合 `CONDA_DEFAULT_ENV` 从 Conda 的 `envs` 目录解析 Python，之后才回退到 `CONDA_PREFIX` 或 PATH；验证脚本使用 `importlib.util.find_spec` 静默确认 pytest/Ruff 可用，再以 `python -m ruff` 加载 Ruff，并显式检查每个外部命令的 `$LASTEXITCODE`。Pytest 使用仓库内每次唯一的 `.test-tmp/` 目录并禁用 cache provider，完成后仅清理本次创建的目录。明确 Durable CLI、Chainlit、MCP Server 和离线预览入口边界。清理源代码与测试中仅表示开发阶段的 Day 1/Day 2 docstring，但保留开发计划、日志和独立 Day 0 Spike 的历史语义。仓库扫描不读取或修改用户维护的 `.env`。
- **修改原因：** 让展示命令可重复、入口职责不会混淆，并确保任一 Ruff、测试、依赖或 stdio MCP 检查失败时一键验证脚本都可靠返回失败。
- **影响范围：** 交付文档、演示和验证脚本以及非功能性模块说明；不改变业务逻辑、Graph State、Artifact Schema、模型配置或 `.env`。
- **验证结果：** `scripts/verify.ps1` 完整通过：45 项 pytest、Ruff lint/format、`pip check` 和独立 stdio MCP smoke 均成功，仅存在 Chainlit 传递依赖的已知 Pydantic 弃用警告。临时数据库与数据目录的端到端验收中，Case A 为 `COMPLETED/LOW`；Case B 启动和新进程查询均为 `WAITING_APPROVAL`，批准后为 `COMPLETED`；fail-first 演示产生 2 条 `RETRY` 和 2 条成功 `TOOL_CALL`，并生成 SQLite Checkpoint、4 个风险 Artifact、JSONL Trace 与最终报告。密钥、Checkpoint、Trace 和过期命名扫描未发现应提交内容。

---

## 记录模板

### YYYY-MM-DD — 阶段/节点名称

- **对应阶段：**
- **文档原约定：**
- **实际情况：**
- **修改内容：**
- **修改原因：**
- **影响范围：**
- **验证结果：**

---

## 2026-08-27 — M0 真实公开案例与验收基线

- **对应阶段：** 场景化能力优化计划 M0 — 真实案例与验收基线。
- **文档原约定：** 建立真实 Case、字段级来源清单、财务指标人工对照答案、预期异常、预期 Research Query 与人工验收点。
- **实际情况：** 用户选定 A 股 `002594` 比亚迪股份有限公司，授权使用公开资料和保存必要信息。2023 年资产负债表字段采用 2024 年年度报告披露的经重述比较数。现有 Financial 路由异常规则仅检查“末年高收入增速且长期现金流下降”与“资产负债率显著上升”；本 Case 在默认阈值下不触发路由异常，但 2025 年归母净利润与经营现金流分别同比下降 18.9677% 与 55.6884%。Risk 层另外依据现金流下降与流动比率低于 1，给出 `HIGH` 且要求人工审核。
- **修改内容：** 新增 `data/case_byd_002594/source/` 真实公开 Case、字段级来源清单、独立财务/异常/证据/风险预期、人工验收表及 M0 回归测试。
- **修改原因：** 将既有 Durable Agent 技术 MVP 锚定到可追溯的真实公开材料，并为 M1 来源校验和 M2 真实搜索提供稳定输入。
- **影响范围：** 新增公开数据与测试 Fixture；未改动既有 Graph、异常阈值、MCP、模型配置或 `.env`。来源清单目前为数据约定，正式 Pydantic Schema 与导入前校验留待 M1。
- **验证结果：** 4 项 M0 专项回归测试通过；Ruff lint/format 已通过。完整验证脚本此前在 pytest 阶段的工具输出被截断，需在本节点收尾时再以本地命令复核完整结果。

- **收尾复核（2026-08-27）：** 全量 pytest 在仓库隔离临时目录中通过（49 passed，1 个既有 `traceloop` Pydantic 弃用警告）；Ruff lint/format 通过；`pip check` 未发现损坏依赖；以 `python -m tests.stdio_research_smoke` 运行的独立 MCP stdio smoke 通过。直接执行 `python tests/stdio_research_smoke.py` 会因 Python 模块搜索路径不含仓库根目录而失败，项目标准入口和 `scripts/verify.ps1` 均使用模块方式，未改动既有实现。

---

## 2026-08-27 — M1 真实材料导入与校验

- **对应阶段：** 场景化能力优化计划 M1 — 真实材料导入与校验。
- **文档原约定：** 提供 `app.case_cli init/validate/run`、`source_manifest.json` Schema、来源/页码/表格/单位/口径校验、财务合理性预检、错误/警告/人工确认分级，并保持既有 Case A/B 兼容。
- **实际情况：** M0 的来源清单为 `source_manifest_v1`，以单个紧凑字段字符串映射一组财务字段，页码和单位写在自由文本位置中；既有 Case A/B 没有来源清单。直接把 v2 元数据设为所有 Case 的硬门槛会破坏既有演示和回归。
- **修改内容：** 新增 `source_manifest_v1/v2` Pydantic Schema、`app.cases` 预检服务和 `app.case_cli`。比亚迪 Case 升级到 v2，为每一组关键财务字段显式记录来源、PDF/印刷页、表格、原始单位、内部单位 `CNY_1000` 与会计口径。新增内存单位归一化，支持 `CNY`、`CNY_10K`、`CNY_1000` 和 `CNY_100M`；新增必需文件、名称/年份/单位、来源覆盖、财务勾稽和可疑数据人工确认检查。`run` 在校验失败时不创建任务。
- **修改原因：** 在保留来源原始单位、支持年报逐项复核和不破坏 A/B 固定回归的前提下，为新公共 Case 建立可机器校验的 v2 契约。
- **影响范围：** M1 新建模板默认使用 `CNY_1000`；预检输出归一化单位及倍率，原始输入文件和既有 Durable Runtime 的读取契约不被静默改写。A/B 在 `case_cli validate` 中返回有效但带迁移警告；它们原有 `task_cli` 路径不变。
- **验证结果：** 待完成完整回归后补充。

- **收尾复核（2026-08-27）：** `scripts/verify.ps1 -PythonExecutable E:\Environment\Anaconda3\envs\env_agent\python.exe` 全部通过：59 项 pytest、Ruff lint/format、`pip check` 和独立 MCP stdio smoke 均成功；仅存在 Chainlit 传递依赖 `traceloop` 的已知 Pydantic 弃用警告。隔离目录中的 `case_cli init` 成功生成模板，`case_cli run` 对比亚迪 v2 Case 先返回 `VALID` 校验结果再进入 `WAITING_APPROVAL`。

- **最终复核（2026-08-28）：** 模板日期改为以 UTC 当前日期生成后，完整门禁再次通过：60 项 pytest、Ruff lint/format、`pip check` 和独立 MCP stdio smoke 均成功；仍仅存在 `traceloop` 的已知 Pydantic 弃用警告。

---

## 2026-09-01 — M2 可插拔互联网搜索与事实核查

- **对应阶段：** 场景化能力优化计划 M2 — Search Provider、Tavily、Snapshot 与 Fact Verification。
- **文档原约定：** 在不改变 `search_company` / `search_industry` MCP Tool 名称的前提下，支持 Mock、Snapshot 和 Tavily；保存真实 URL、来源等级、时间、摘要、相关度与哈希；Web 失败不得静默回退 Mock；默认测试不得消耗额度。
- **实际情况：** 用户要求运行默认值改为 Tavily，而计划示例原为 `RESEARCH_PROVIDER=mock`。开发阶段首次检查时尚未提供 `TAVILY_API_KEY`；用户随后在本轮验收中补齐配置，并成功执行了一次真实 Tavily smoke。若严格按“第一轮 A/B、第二轮媒体/行业”对 8 个确定性查询分别调用两轮搜索，单次完整 Case 至少需要 16 次请求，演示成本偏高。
- **修改内容：** 新增可注册 Provider 工厂与统一错误层，内置 Mock、Snapshot、Tavily；新增 7 个公司和 1 个行业确定性 Query、来源域名分级、排序、内容哈希、主体/类别语义匹配、最低相关度门槛、Evidence Schema 和独立域名同内容的 `CORROBORATED` 判定。Tavily 使用 Bearer HTTP API，关闭 Answer/Raw Content；成功结果可写入脱敏快照。运行默认改为 Tavily，pytest 与 stdio smoke 显式固定 Mock；MCP 子进程显式继承父进程环境。Risk Evidence 同时保留来源 ID 与真实 URL。
- **修改原因：** 满足搜索引擎可替换与当前默认 Tavily 的需求，避免测试误耗额度，并让缺 Key、401、429、额度、超时、网络和快照缺失成为可审计失败。为控制完整场景调用量，第一版对每个确定性 Query 仅调用一次 Tavily，再按 A/B/C 等级后排序，而不是执行两轮重复搜索；该适应性修改把一次完整调查控制为 8 次请求。
- **影响范围：** Research 配置、MCP Server/Client、Research Artifact Schema、Risk Evidence、测试和演示说明；开发过程未修改 `.env` 且未输出任何真实配置值，真实 Key 不进入 Trace、Artifact 或快照。低等级结果保留为 `UNVERIFIED` Evidence，不进入确定事实列表；Mock 模式保留原有固定事实行为。
- **验证结果：** Ruff lint/format、72 项 pytest、`pip check` 和独立 MCP stdio smoke 全部通过；仅保留 `traceloop` 传递依赖的既有 Pydantic 弃用警告。Tavily HTTP 映射、真实 URL Evidence、401/429/432/500、超时、Snapshot 往返/缺失、Provider 注册和确定性 Query 均由 MockTransport/离线测试覆盖。真实 Tavily smoke 已显式执行一次：监管处罚 Query 返回 5 个真实 URL，生成的 JSON 快照可解析并可由 SnapshotStore 回放，布尔审计确认不含 API Key；根据真实结果补充 `cnstock.com` 与 `zqrb.cn` 为 B 级来源；进一步审计发现可信来源并不等于语义支持，因此新增主体、类别关键词和 `0.5` 最低相关度门槛。这次 5 条结果均被谨慎保留为 `UNVERIFIED`，没有进入 Risk 确定事实。同时通过 `protected_namespaces=("settings_",)` 消除用户 base 环境中旧 Pydantic 对 `model_*` 字段的警告。
---

## 2026-09-01 — M2.1 搜索过滤后续优化计划

- **对应阶段：** 场景化能力优化计划 M2.1 — 搜索结果过滤与深度事实核验。
- **文档原约定：** M2 以 Provider 真实搜索、Snapshot、来源等级和基础核验状态为完成边界，尚未展开 URL 正文抓取与大模型逐条核验的实施方案。
- **实际情况：** 比亚迪“债务逾期”真实 Query 返回了高相关度的华谊兄弟文章。现有主体/类别规则能够将其标记为 `query_match=false`、`UNVERIFIED` 并排除出 `facts`，但 Raw Snapshot 容易被误解为已核实事实；搜索分数、可信来源和关键词命中也不足以证明正文支持待核查结论。
- **修改内容：** 经用户明确要求，在优化计划新增 M2.1，并加入阶段总览。方案将产物分为 Raw Search、Candidate Evidence 和 Verified Fact 三层，规划实体别名与类别过滤、HTML/PDF 正文抓取、结构化 Claim、大模型 `SUPPORTS/REFUTES/IRRELEVANT/INSUFFICIENT` 核验、独立信源聚合、冲突处理、缓存/成本预算、提示注入隔离和负向回归。
- **修改原因：** 防止无关企业、主题近似文章和可信但不支持结论的来源进入 Risk/Report，使确定事实能够回查到正文证据位置，而不只依赖标题、摘要、来源等级或搜索分数。
- **影响范围：** 本次仅更新优化计划和开发日志，不修改代码、运行配置或 `.env`。后续实现预计涉及 Search Evidence Schema、ContentFetcher、Verifier、Artifact 分层、Trace、Risk/Report 消费边界和 M3 展示。
- **验证结果：** M2.1 已列入阶段总览，并包含问题边界、目标链路、确定性过滤、正文获取、大模型核验、多来源聚合、成本配置、测试矩阵和完成定义；华谊兄弟误召回已被指定为必须长期通过的负向回归 Case。

---

## 2026-09-01 — M2.1-A 确定性过滤与产物分层

- **对应阶段：** M2.1-A — Raw Search、Candidate Evidence 与 Verified Fact 分层，主体/类别确定性过滤及负向回归。
- **文档原约定：** 以公司全称、简称、证券代码和行业别名建立实体词表；在正文抓取和大模型核验前执行主体、类别、相关度与重复内容过滤；保留过滤原因，并将“比亚迪查询召回华谊兄弟文章”固化为负向回归。
- **实际情况：** M2 实现曾把通过主体/类别门槛的 A/B 级搜索摘要直接标记为 `SUPPORTED`，并把相同内容的跨域结果自动升级为 `CORROBORATED`；去重时还会丢弃被淘汰项，无法完整审计过滤路径。新增主体别名若直接参与 Snapshot Key 计算，会使既有真实快照全部失配。当前 Company Profile 未包含证券代码，公开 MCP Tool 也只接收公司名称，因此本阶段可自动派生公司简称和行业基础名称，并预留显式别名入口；证券代码从 Case 来源清单自动注入留待不改变公共调用契约的后续适配。
- **修改内容：** 为 Search Request 增加主体别名，为 Evidence 增加 `subject_match`、`category_match`、`filter_reasons` 和 `evidence_stage`；将所有 Web/Tavily 与 Snapshot 结果限制为 `CANDIDATE/UNVERIFIED`，只有显式 Mock 可信夹具可在离线测试中直接成为 `VERIFIED`。Research Artifact 新增候选证据、Raw/Candidate/Verified/Rejected 计数，以及相互独立的 `execution_status` 与 `verification_status`。去重改为保留全部结果并给落选项标记 `DUPLICATE_CONTENT`，移除仅凭摘要哈希跨域相同就自动核验的行为。Risk 再次校验事实状态，确保候选和拒绝结果不能进入 Risk/Report。Snapshot Key 排除仅影响下游过滤的别名字段，并在回放时应用当前请求别名，从而兼容既有快照。
- **修改原因：** 搜索分数、来源等级和摘要关键词都不能证明 URL 正文支持某个事实；必须在数据模型和消费边界上阻止未核验结果被误当作事实，同时完整保留拒绝原因供演示和审计。
- **影响范围：** Search/Research Artifact Schema 采用向后兼容的新增字段；实时 Web 调查在 M2.1-C 正文核验完成前可能执行成功但 `facts=0`，这会通过 `verification_status` 明确呈现。MCP Tool 名称和既有调用方式不变，`.env` 未修改。显式 Mock 可信夹具仍维持离线纵向链路回归。
- **验证结果：** 新增主体不匹配、类别不匹配、别名候选、重复内容、候选与执行状态分离、Snapshot Key 兼容和 Snapshot 当前别名回放测试。华谊兄弟负向夹具在单元、Snapshot 集成和 Research→Risk→Report 纵向测试中均被拒绝。完整门禁通过：Ruff lint/format、80 项 pytest、`pip check` 和独立 MCP stdio smoke 均成功，仅保留 `traceloop` 传递依赖的既有 Pydantic 弃用警告。用本地既有真实快照离线复核得到 Raw 32、Candidate 3、Rejected 29、Verified Fact 0；华谊兄弟条目为 `REJECTED`，拒绝原因为 `SUBJECT_MISMATCH`。

---

## 2026-09-01 — 项目目标对齐审查与路线校准

- **对应阶段：** 场景化能力优化计划的阶段性目标审查，以及 M2.1 后续至 M3 前的路线校准。
- **文档原约定：** 原始 MVP 以 Durable Agent Runtime 为核心；场景化计划按 M0 真实 Case、M1 导入校验、M2/M2.1 互联网事实核查、M3 工作台、M4 LLM 表达和 M5 Eval/审计包逐步形成可信尽调闭环。
- **实际情况：** Runtime、Checkpoint/Resume、HITL、Artifact、MCP、Retry/Trace、真实结构化 Case、可插拔搜索和 M2.1-A 候选过滤已经形成稳定链路，项目没有偏离 Durable Agent 技术 MVP 的主轴。但当前 Research 不根据异常类型或人工审核意见选择查询，补充调查会重复固定查询；已核验外部 Evidence 被统一附加到财务风险项而缺少类别语义映射；Document 节点只校验 `business_info.md` 非空；运行 Artifact 按 Case 而非 Thread 隔离；不可恢复错误未形成稳定可查询的 `FAILED` 状态。真实 Web 候选尚未完成正文核验，因此项目不能表述为已完成可信真实企业尽调。
- **修改内容：** 在优化计划新增目标对齐审查结论和当前准确定位；新增 M2.2“调查语义闭环与运行隔离”，规划 Investigation Intent、Query Planning、Evidence-Risk 映射、任务级 Artifact/Report、FAILED 生命周期、Document 能力边界和文档同步；在总体验收标准增加调查意图、风险语义、任务隔离和失败状态门禁；将后续顺序调整为 M2.1-B、M2.1-C、M2.2、比亚迪真实纵向验收之后再进入 M3 工作台。
- **修改原因：** 防止界面优化掩盖事实核验、业务消费和任务隔离尚未闭合的问题，并确保项目对外表述与可验证能力一致。
- **影响范围：** 本次为正式计划基线校准，只修改优化计划和开发日志；不修改代码、运行配置、`.env` 或现有 Artifact。后续 M2.2 将涉及 Research Query、Risk Schema/规则、Artifact 路径、Task 状态和相关测试，实施前仍需按阶段逐项开发与验收。
- **验证结果：** 已对原始需求、开发计划、场景化计划、主工作流、Research/Risk/Report、Artifact/State、关键集成测试和 README 进行只读交叉审查；新增阶段与总体验收、建议顺序保持一致。文档修改完成后执行 Git 差异格式检查，不运行代码测试。
---

## 2026-09-01 — M2.1-B 受控 URL 正文抓取与内容定位

- **对应阶段：** M2.1-B — 对 Candidate Evidence 执行受控 URL 获取，提取 HTML 段落与 PDF 页码，生成可离线回放的正文快照和小型引用元数据。
- **文档原约定：** 只允许公网 `http/https`，校验跳转目标并限制超时、正文大小、PDF 页数和并发；提取标题、发布日期、正文位置与内容哈希；显式处理超时、登录墙、重定向、超大正文、异常 PDF 和页面提示注入；正文视为不可信输入，在 M2.1-C 核验前不得进入 Risk/Report。
- **实际情况：** 搜索 Provider 只返回标题和摘要，无法提供稳定的正文定位；HTML 来源结构不统一，PDF 可能加密、无文本或过大。生产路径需要让 Tavily 自动使用联网抓取、Snapshot 完全离线回放、Mock 测试不触网；同时要区分“搜索 MCP 执行失败”和“候选正文获取不完整”，避免重试已成功的付费搜索。直接 PDF 文本提取需要增加显式运行依赖。
- **修改内容：** 新增 `ContentFetcher` 协议、HTTP 与 Snapshot 实现、`FetchedDocument` / `FetchedContentReference` 契约和 `data/content_snapshots/` 运行时存储；仅抓取 `CANDIDATE` URL，按候选预算并发执行，并在 Research Artifact 中只保留 URL、类型、标题、日期、哈希、段落/页码位置、快照 ID、提示注入信号和分类错误。公网检查覆盖字面 IP、DNS 结果、私网地址、非法端口和每次重定向，禁止 HTTPS 降级到 HTTP；响应限制覆盖声明大小、实际流式字节、正文字符和 PDF 页数。HTML 使用标准库解析，PDF 使用新增直接依赖 `pypdf==6.16.2`。`.env.example` 增加可插拔抓取器与预算配置，运行时正文目录加入 `.gitignore`；未修改 `.env`。
- **修改原因：** 为下一阶段 Claim/Verifier 提供可回查的正文位置，并防止无关搜索摘要、私网 URL、异常内容、登录墙或页面指令被静默当作事实。HTTP 成功与失败混合时聚合为 `PARTIAL`；正文失败会设置 `content_fetch_incomplete` / `external_research_incomplete`，但不会把已成功搜索错误计为 MCP Tool 失败。
- **影响范围：** Search Evidence 与 Research Artifact 采用向后兼容的新增字段；Tavily 模式默认抓取候选正文，Snapshot 模式同时要求搜索快照和正文快照，Mock 保持纯离线。完整正文只存在于已被 Git 忽略的运行时快照中，Risk/Report 仍只消费既有 Verified Fact；M2.1-C 完成前，真实 Web 候选即使正文抓取成功也保持 `UNVERIFIED`。当前 HTML 提取不执行 JavaScript，PDF 不提供 OCR，相关受限来源会显式失败或无文本，不做隐式猜测。
- **验证结果：** 新增 HTML 标题/日期/段落、PDF 页码、快照回放、私网与重定向拦截、IPv6/非法端口、HTTPS 降级、类型/大小/登录墙/超时、候选预算、正文隔离、缺失快照、Research 状态传播和成功/失败混合聚合测试；真实正文 smoke 需由用户以显式环境开关指定 1 个公开 URL，默认测试不会联网。完整门禁通过：Ruff lint/format、93 项 pytest、`pip check` 和独立 MCP stdio smoke 均成功，仅保留 `traceloop` 传递依赖的既有 Pydantic 弃用警告。
- **真实验收补充（2026-09-01）：** 新浪财经公开研报 HTML 抓取成功，得到正确的比亚迪研报标题、993 个正文字符、40 个段落位置和稳定 SHA-256；核心内容位于第 19–27 段，未发现提示注入信号。真实页面同时暴露两项非阻断质量边界：通用 HTML 提取仍包含导航/页脚噪声，页面正文日期未写入 Meta 时 `published_at` 为空，后续正文精炼与日期识别应继续优化。
- **集成验收修复（2026-09-01）：** 主 Case 重复运行首先复现了已列入 M2.2 的 Case 级 Artifact 冲突；改用隔离 `data_dir` 和本轮生成的 8 个搜索快照、11 个正文快照后，Research 成功生成，但 `content_fetch_status=PARTIAL`、`failed_tools=[]` 使既有 Risk 规则构造出空证据并被 Schema 拒绝。修复 Risk 不完整调查证据映射：正文不完整时记录 `content_fetch_status:<STATUS>` 与失败正文数，工具失败和正文失败可同时记录，并为旧 Artifact 保留非空兜底引用。修复后隔离 Snapshot 纵向回放成功进入第二次 `WAITING_APPROVAL`，Risk Artifact 升级到 v2，外部调查风险项包含 `content_fetch_status:PARTIAL` 和 `content_fetch_failed:3`。完整门禁再次通过：Ruff lint/format、93 项 pytest、`pip check` 和独立 MCP stdio smoke 均成功。

---

## 2026-09-01 — M2.1-C 结构化 Claim、正文核验与跨来源聚合

- **对应阶段：** M2.1-C — 对已抓取 Candidate Evidence 生成稳定 Claim，执行规则或模型正文核验，校验证据短句与位置，并按来源可靠性、独立性和冲突关系生成最终事实状态。
- **文档原约定：** Verifier 至少输出 `SUPPORTS`、`REFUTES`、`IRRELEVANT`、`INSUFFICIENT`；直接支持或反驳必须回查正文短句和 HTML 段落/PDF 页码。只有 A/B 级可靠来源可形成 `SUPPORTED`，两个真正独立来源形成 `CORROBORATED`，可靠来源冲突形成 `CONFLICTING`；未经正文核验的 Web 结果不得进入 Risk/Report。默认回归使用固定 Snapshot 与 Mock Verifier，不得联网或调用真实模型。
- **实际情况：** 工程已有 OpenAI-compatible 模型配置和 SDK 依赖，但没有通用模型调用适配器。搜索摘要本身不适合作为稳定 Claim，因此需要按主体和调查类别确定性生成 Claim，避免不同来源的摘要措辞阻止聚合。现有 MCP 公共接口将公司和行业作为两个 Tool 独立执行，无法在不改变 Tool 契约和运行时状态的情况下共享严格的单 Case 模型调用计数。
- **修改内容：** 新增 Claim、Verifier Decision、Claim Verification 和执行状态 Schema；实现 `rules`、测试专用 `mock`、OpenAI-compatible `llm` 三类 Verifier，以及显式 `disabled` 模式。规则模式只接受正文中的完整规范化 Claim 文本，不作语义猜测；LLM 使用 JSON-only 输出、无 Tool 权限、受限正文片段和提示注入隔离指令。核验结果必须保持原 Claim，`SUPPORTS/REFUTES` 的短证据必须在指定位置回查；低置信度、Claim 漂移、位置不匹配、模型错误、正文缺失和预算超限均显式降级。新增按 URL、正文哈希、Claim、模型、Prompt 版本和置信度门槛键控的忽略式运行缓存；聚合要求 A/B 级来源，跨域且正文哈希不同才算独立印证，同稿转载不升级，支持与反驳并存时不自动选边。`CONFLICTING` 不进入事实，并生成带双方 URL/位置的 `external_research_conflict` 风险项，报告明确披露系统未自动选边。Research Artifact、Trace 摘要、Risk 证据和 Report 现在保留 Claim ID、URL、证据位置、模型版本与核验时间；`.env.example` 增加配置，`.env` 未修改。
- **修改原因：** 防止模型根据搜索标题、来源等级或搜索分数生成事实，确保每条确定事实都能回到已保存正文；同时维持 Mock 纵向回归和 Rules 离线降级，不让模型、缓存或额度失败静默变成肯定结论。
- **影响范围：** Search Evidence 与 Research Artifact 采用向后兼容的新增字段；Mock 可信夹具仍可维持既有离线事实回归。`FACT_VERIFIER_MAX_CANDIDATES` 当前按一次 MCP Tool 调用生效，公司与行业完整调查的理论上限是配置值的两倍；严格的 Case 级共享预算需要 M2.2 的任务上下文与运行隔离后实现。README 的整体架构图同步仍保留到 M2.2，当前演示指南已经准确说明 M2.1-C 能力。真实 LLM 模式复用用户维护的模型配置，开发过程未读取或输出真实 Key。
- **验证结果：** 新增稳定 Claim、规则严格匹配、结构化 LLM 输出、提示注入隔离、证据位置回查、低置信度降级、Verifier 无关判定、C 级来源限制、独立来源印证、同稿转载、支持/反驳冲突、缓存、候选预算、超长单段输入截断、工厂配置、Research 状态传播和 Verified Fact→Risk→Report 追溯测试。使用已有搜索与正文快照执行 `rules` 隔离纵向回放：10 个候选完成核验，5 个候选因 Tool 级预算明确标记 `BUDGET_EXCEEDED`，所有非完全匹配结果保持 `INSUFFICIENT/UNVERIFIED`，任务成功进入第二次 `WAITING_APPROVAL`。完整门禁通过：Ruff lint/format、105 项 pytest、`pip check` 和独立 MCP stdio smoke 均成功，仅保留 `traceloop` 传递依赖的既有 Pydantic 弃用警告。真实 LLM smoke 已提供显式开关，本阶段未擅自消耗模型额度。

---

## 2026-09-02 — M2.2 调查语义闭环与运行隔离

- **对应阶段：** M2.2 — Investigation Intent、规则化 Query Planning、Evidence-Risk 语义映射、任务级运行隔离、持久化失败状态及能力表述同步。
- **文档原约定：** 将异常标记、当前风险、人工决策和审核意见归一化为确定性调查意图；只执行相关调查类别并记录 Query 版本差异；Verified Fact 只进入同类别外部风险项；同一 Case 的不同任务按 Thread 或等价 Run ID 隔离；不可恢复节点错误必须持久化为可查询的 `FAILED`；Document 节点只表述为结构化输入校验与归一化。
- **实际情况：** 原实现每次固定执行 7 个企业查询与 1 个行业查询，人工意见不影响查询；所有外部 Evidence 被追加到资产负债率和现金流风险；Artifact 与报告写在 Case 根目录，重复任务会发生不可变文件冲突；节点异常只抛出到 CLI，Checkpoint 中没有稳定终止原因。M2.1-C 的既有 Mock 数据还使用 `receivables`、`financing`、`industry_cashflow` 和 `industry_demand` 历史类别，需要显式兼容映射。
- **修改内容：** 新增 `InvestigationIntent`、Trigger 和 `QueryPlan` Schema 及纯规则规划器，按异常、风险类型与人工意见关键词选择企业或行业类别；MCP Tool 增加可选 `categories` 参数且保留旧调用方式，未被选中的行业调查显式记为 `not_requested`。Query Artifact 保存 Intent、查询文本、上一 Plan ID 以及增删列表，Trace 记录受限人工意见摘要和类别。Verified Fact 增加稳定 `fact_id`、主体、Claim ID 与 Relation；Risk 将监管、诉讼、债务、造假、业绩、控制人、经营和行业事实分别映射为独立风险项，财务风险恢复为只引用相关指标，冲突、未核验和未找到仅形成冲突或证据缺口披露。新任务使用 Thread ID 哈希得到文件系统安全的 `run_id`，运行产物写入 `data/<case_id>/runs/<run_id>/`；旧 Checkpoint 缺少 `run_id` 时继续读取原 Case 级目录，不迁移既有数据。节点边界捕获不可恢复异常并持久化 `FAILED`、`failed_node` 与脱敏截断错误摘要，同时排除 LangGraph 正常 Interrupt；Research 的可恢复 Tool/正文/核验失败仍保持 `INCOMPLETE`。README、架构图、演示指南、项目展示说明、演示脚本和 `.gitignore` 已同步；`.env` 未修改。
- **修改原因：** 让“补充调查”真正响应当前风险与人工意图，阻止无关证据污染财务风险，允许同一公开 Case 安全重复演示，并让 UI/CLI 能解释不可恢复终止原因。历史 Mock 类别通过明确别名映射到现行风险类别，避免以静默宽松规则破坏既有回归。
- **影响范围：** 新任务的 Artifact/Report 路径由 Case 根目录变为 `runs/<run_id>/`，State 新增 Run、Intent、Query、Report 和失败字段；Artifact 内部引用仍保持 `artifacts/<name>_vN.json`，同任务 Resume 继续生成 v2 并保留 v1。`source/` 仍是 Case 级只读输入；搜索、正文和核验 Snapshot 仍是可回放缓存，不被误表述为任务业务产物。MCP 的旧单参数调用和旧 Checkpoint 目录保持兼容。
- **验证结果：** 新增并通过同 Intent 查询确定性、人工监管意见改变类别、Query 增删差异、监管/诉讼事实精确映射、财务证据不混入外部来源、同 Case 双 Thread Artifact/Report 隔离、不可恢复 Document 错误持久化与跨进程查询测试；更新既有 Runtime、Resume、Retry 与路径断言。完整门禁通过：Ruff lint/format、109 项 pytest、`pip check` 和独立 MCP stdio smoke 全部成功；仅保留 `traceloop` 传递依赖的既有 Pydantic 弃用警告。另以隔离目录对比亚迪 Case 执行既有 Snapshot + Rules 纵向验收：人工意见生成 `operations/regulatory/debt` 三类 Query，获得 9 条候选、拒绝 6 条，正文抓取与核验均为 `PARTIAL`，未形成 Verified Fact，因而 Risk 只新增 `external_research` 证据缺口而没有生成肯定外部风险；任务经第二次 HITL 后进入 `COMPLETED` 并在独立 Run 目录生成报告。该结果验证了意图、隔离、保守降级和报告链路，也明确说明现有快照不足以形成正向事实结论。全程未访问 Tavily、不调用真实 LLM，也未读取或输出用户 API Key。

---

## 2026-09-02 — M3-A Chainlit 工作台入口与任务状态总览

- **对应阶段：** M3-A — 在现有 Chainlit 入口上实现 Case 选择、结构化预检、任务状态总览、节点进度、Artifact 引用、HITL 操作和已有 Thread 恢复。
- **文档原约定：** M3 保留 CLI 作为可靠降级入口，Chainlit 与 CLI 共用 Durable Runtime；非开发人员无需手写 Runtime 命令即可启动 Case，并能理解当前状态、暂停原因和下一步操作。M3-A 先完成工作台入口与状态视图，Evidence/Trace 详情和报告下载留到 M3-B/C。
- **实际情况：** 原 Chainlit 页面只显示三条文本命令提示，启动 Case 不执行 M1 预检，状态只展示 Task ID、状态、当前节点和风险等级；Action 使用固定评论。浏览器联调还发现 Chainlit 回调处于 Async Event Loop，而同步 Research MCP Client 明确拒绝在活动事件循环中调用，导致风险 Case 的 Research 节点持久化为 `FAILED`。CLI 和非异步测试不会暴露该问题。
- **修改内容：** 首页自动发现合法 Case 并执行只读预检，以表格和按钮展示可启动项；启动前强制通过 `validate_case`，任务面板展示 Case、Thread、Run、原始状态码、当前/下一节点、风险等级、输入警告/确认项、六节点进度和 Artifact 引用。新增通用 Case 启动、任务恢复、状态刷新、批准和补充调查 Action；HITL Action 先收集人工意见再 Resume，文本命令继续兼容。`FAILED` 状态显示失败节点和安全摘要。所有同步 Runtime 调用通过 `asyncio.to_thread` 从 Chainlit Event Loop 隔离，核心 Runtime/MCP 契约不变；错误提示沿用 API Key/Authorization/Bearer 脱敏。未修改 `.env`。
- **修改原因：** 让非开发人员可通过本地 Web 页面启动和恢复 Durable Task，并修复真实 Chainlit 风险链路中的同步/异步边界冲突，而不是在 MCP Client 内嵌套事件循环或复制一套 UI 状态。
- **影响范围：** 主要影响 `app/chainlit_app.py`、Chainlit 渲染测试、README 和演示文档；`app.task_cli`、SQLite Checkpoint、Artifact、Graph State 和 Research Tool 协议不变。M3-A 仍使用 Chainlit 原生聊天布局，不引入 FastAPI、React 或独立数据库；Evidence 展开、Trace 时间线和报告下载尚未实现。
- **验证结果：** 新增 Case 发现/预检、状态总览、节点状态推导和 `FAILED` 展示测试，定向测试 3 项通过。使用本地 Chainlit + Mock 模式完成浏览器验收：首页正确显示 3 个 Case 及预检状态；正常 Case 进入 `COMPLETED` 并显示 Research/Approval 跳过；修复 Event Loop 冲突后风险 Case 正常进入 `WAITING_APPROVAL`，显示批准/补充调查按钮；提交人工意见后进入 `COMPLETED` 并显示 Report Artifact。浏览器验收未访问 Tavily 或真实模型。

---

## 2026-09-02 — M3-B Evidence、Artifact 版本与 Retry/Trace 可视化

- **对应阶段：** M3-B — 在现有 Chainlit 任务面板中展示 Investigation Intent、确定性 Query、Research Evidence、正文/Verifier 状态、Artifact 版本变化和 Retry/Trace 摘要。
- **文档原约定：** M3 工作台需要展示 Research Query、Evidence、来源等级、可点击 URL、Artifact 版本变化，以及 Retry、Interrupt 和 Resume 摘要；Evidence URL 应可直接打开核验，UI 与 CLI 继续共用 Durable Runtime。
- **实际情况：** Research Artifact 可能超过 100 KB，并同时包含候选、拒绝和核验结果；Trace 会随状态查询和 Resume 持续增长。外部标题、摘要和 URL 均是不可信输入，不能直接拼入 Markdown。首次浏览器联调还发现列表后缺少 CommonMark 空行会使 Query/Trace 表格退化为普通文本；沙箱内 Chainlit 子进程权限不足会让 Mock MCP 形成显式证据缺口，但沙箱外本地服务可以正常完成链路。
- **修改内容：** 新增只读 Workbench Projection，严格按 State 中的 Case/Run 和 Artifact 引用读取当前 Query Plan、Research Artifact 与任务 Trace；展示 Intent 类别、确定性 Query、执行/正文/核验状态、Evidence 分层、来源等级、主体/类别匹配、过滤原因、Claim、Verifier Relation、置信度、证据位置/短句和降级原因。Artifact 目录按安全文件名聚合版本历史；Trace 校验 `TraceEvent` Schema，汇总 Retry/Failed/Interrupt/Resume 并展示最近事件。Evidence 默认显示前 12 条，Trace 最多读取 512 KB/200 项并显示最近 16 项；所有截断均明确披露。外部文本执行单行化、长度限制和 Markdown 转义；只为无凭据、非 localhost、非字面私网地址的 `http/https` URL 生成链接。未读取正文快照，未修改 `.env`。
- **修改原因：** 让审核人员在不打开 JSON Artifact 的情况下判断搜索是否执行、候选为何被拒绝、正文和核验在哪里降级，以及任务经历了哪些重试和人工恢复；同时避免大型产物阻塞页面或不可信来源注入 Markdown/危险链接。
- **影响范围：** 新增 `app/workbench.py` 并扩展 Chainlit 渲染边界；Runtime、Checkpoint、Graph State、MCP Tool、Research/Trace Schema 和 CLI 均未改变。工作台读取失败会显示类型化提示，不影响任务状态；M3-C 报告预览和下载仍未实现。
- **验证结果：** 新增 Query/Evidence/安全链接、Artifact 版本、Retry/Interrupt/Resume、Trace 路径穿越和私网 URL 拦截测试；M3-B 与 Chainlit 定向测试 6 项通过，联合 HITL/Retry 回归 15 项通过。浏览器以 Mock 风险 Case 验证确定性 Query、2 条 Mock Evidence 和完整 Trace；恢复比亚迪 Snapshot Thread 后正确展示 10 条真实 URL Evidence、正文 `PARTIAL`、核验 `UNVERIFIED`、降级原因、Risk v1→v2，以及 25 个 Trace 事件中的 3 次 Interrupt 和 2 次 Resume。浏览器验收未访问 Tavily 或真实模型。完整门禁通过：Ruff lint/format、114 项 pytest、`pip check` 和独立 MCP stdio smoke 全部成功；仅保留 `traceloop` 传递依赖的既有 Pydantic 弃用警告。

---

## 2026-09-02 — M4 计划校准与 M4-A 主链路受约束风险解释

- **对应阶段：** 场景化能力优化计划 M4-A — 通用 OpenAI-compatible 结构化模型网关与主链路 Risk Narrative；同时细化 M4-B 至 M4-D 的后续边界。
- **文档原约定：** 早期 M4 仅以“LLM 驱动解释/表达层”概述后续工作，且 Day 1 基线明确要求财务计算、风险等级和路由不依赖模型。模型接入需可回退、可审计，并不能替代人工审核。
- **实际情况：** 现有模型能力主要集中在 M2.1-C 的单文档 Fact Verifier，主任务的 `Risk` 到 `HITL` 路径仍完全确定性；直接将模型输出接入 Risk 或 Report 会使风险结论、证据边界和回归稳定性发生耦合。项目已有用户维护的 OpenAI-compatible 基础模型配置，但默认测试和本轮开发不应消耗真实模型额度。
- **修改内容：** 将 M4 正式拆分为 M4-A（引用受限的风险解释）、M4-B（Evidence 摘要和受限 Query 建议）、M4-C（受限报告表达和 Unsupported Claim 检查）、M4-D（真实模型 Smoke/Eval/成本稳定性）。本轮实现 M4-A：新增严格的风险解释 Schema、无 Tool 权限的 JSON-only 通用模型网关、提示注入隔离 Prompt、`risk_narrative_vN.json` Run Artifact、`LLM_CALL` / `LLM_SKIP` Trace，以及配置/模型/格式/证据不合法时保留确定性风险描述的 `DEGRADED` 降级。Graph State、CLI Runtime 和 Chainlit 均能引用该 Artifact；工作台只显示已校验的解释、允许证据、执行元数据和错误分类，不显示原始错误、Prompt 或模型原文。`.env.example`、README 和演示指南增加了配置和可控的真实主链路验收说明；`.env` 未修改。
- **修改原因：** 先让模型在“解释已知、可追溯风险”这一低权限位置产生可观测价值，再逐步扩展到调查建议和报告表达；任何模型异常均不应让系统生成未经证据约束的新事实，或改变 `risk_level`、动态路由、HITL 条件与最终授信判断。
- **影响范围：** 新增 `app/llm/`、风险解释模型和 Prompt；Risk 节点在每次执行后新增版本化解释 Artifact，默认 `deterministic` 模式标记为 `NOT_REQUESTED`，`llm` 模式仅在有风险项时调用模型。既有财务、Research、Risk、Report、Checkpoint 兼容路径不变；最终报告尚不消费模型解释，留待 M4-C。为避免兼容 Provider 的构造参数异常中断任务，工作流将 `TypeError`/`ValueError` 归类为安全的 `CONFIG_ERROR` 降级。
- **验证结果：** 新增网关重试、配置缺失、超长输入、非法 JSON、虚构/越权证据、确定性无调用、主链路 HITL 不变、降级不阻断、工作台展示和原始错误隐藏测试。定向回归 18 项通过；完整门禁通过：Ruff lint/format、127 项 pytest、`pip check` 和独立 MCP stdio smoke 均成功，仅保留既有 `traceloop` Pydantic 弃用警告。以本地 Chainlit、Mock Research 和 `ANALYSIS_MODE=deterministic` 进行了浏览器验收：风险任务仍进入 `WAITING_APPROVAL`，页面正确展示 `LLM Narrative` Artifact、`NOT_REQUESTED` 状态、允许证据和版本历史。该验收未调用 Tavily 或真实模型，也未读取或输出用户 API Key。

---

## 2026-09-02 — M4-B Evidence Summary 与受约束 Query Proposal

- **对应阶段：** 场景化能力优化计划 M4-B — 在 Research 节点内基于结构化调查结果生成 Evidence Summary 与仅供人工审核的 Query Proposal。
- **文档原约定：** 模型必须区分已核验事实、冲突证据、未核验候选和证据缺口；只能在 Investigation Intent 已授权类别内提出查询文案；摘要和建议必须绑定已存在的 Claim、Fact、Source、Evidence 或 Gap 标识。规则 Query Plan 保持基线，模型建议不得自动执行、修改路由或破坏 Snapshot/Mock 离线回放。
- **实际情况：** M4-A 已提供通用 OpenAI-compatible 结构化模型网关，可以复用同一调用和降级边界。Research Artifact 同时包含候选、拒绝、核验和事实数据，若直接交给模型会扩大不可信输入和提示注入面；旧 Mock/Snapshot 还保留 `receivables`、`financing`、`industry_cashflow`、`industry_demand` 历史类别，首次工作台验收因此把已有债务候选误判为 `debt` 类无候选缺口。
- **修改内容：** 新增严格的 Evidence Summary、Evidence Gap、Query Proposal Draft/Artifact Schema 和独立 Prompt；只从 Candidate Evidence、Verified Fact 与规则 Query Plan 构造受限结构化索引，不读取 Rejected Evidence 或完整抓取正文。一次结构化模型调用生成两类产物，随后分别写入 `evidence_summary_vN.json` 与 `query_proposal_vN.json`；模型不得提升 `UNVERIFIED/CONFLICTING` 状态，Verified 摘要必须完整且唯一覆盖允许的 Evidence ID。Query Proposal 通过确定性门禁校验类别、查询类型、主体、Evidence/Source/Gap 引用、基线重复、建议重复和格式，只能得到 `ACCEPTED_FOR_REVIEW` 或 `REJECTED`，不会执行搜索或改写 `query_plan_vN.json`。State、Trace、Chainlit Artifact 列表和 Workbench 增加对应引用与安全展示；Trace 仅保存用途、模型、Prompt 版本、状态、尝试次数和 Token 统计。集中提取历史 Research 类别归一化函数，由 Risk 与 M4-B 共用；Artifact 保留原始 `source_category` 供审计。README、优化计划和演示指南同步当前能力与真实模型验收命令；`.env` 未修改。
- **修改原因：** 让模型在不获得 Tool 权限、不接触未受控正文且不改变确定性事实和查询计划的前提下，为审核人员提供可读的证据摘要和后续调查建议；集中式类别归一化用于消除历史夹具与现行调查分类之间的语义差异，避免界面产生虚假证据缺口。
- **影响范围：** Research 节点每次执行新增两个版本化 Run Artifact，Graph State 增加对应引用；`ANALYSIS_MODE=deterministic` 时完全不调用模型并生成确定性 `NOT_REQUESTED` 产物，`llm` 模式发生配置、模型、Schema、状态或引用错误时两个产物共同降级为 `DEGRADED`，原 Research、Query Plan、Risk、HITL 和 Report 链路继续执行。历史类别的规范化只影响分类解释与风险归组，不修改原始 Evidence、Fact、URL 或来源等级。最终报告尚不消费这些模型表达，仍属于 M4-C 范围。
- **验证结果：** 新增确定性零调用、合法结构化摘要、状态提升拦截、历史类别映射、越权/重复/未知引用建议拒绝、主链路 Artifact/Trace/HITL 保持和工作台安全展示测试；更新 M4-A Trace 断言以区分同一任务中的不同 LLM 用途。收口时完整 134 项 pytest 通过；类别归一化修复后相关 M4-B、M2.2 与 Workbench 19 项回归再次通过。Ruff lint/format、`pip check` 与独立 MCP stdio smoke 均成功，仅保留既有 `traceloop` Pydantic 弃用警告。使用本地 Chainlit、Mock Research 和 `ANALYSIS_MODE=deterministic` 完成浏览器验收：页面展示 Evidence Summary、证据缺口与只读 Query Proposal，修复后的债务历史类别不再产生虚假 `debt` 缺口，任务仍停在 `WAITING_APPROVAL`。本阶段未访问 Tavily、未调用真实 Qwen，也未读取或输出用户 API Key；真实模型纵向验收保留到 M4-D。

---

## 2026-09-02 — M4-D 首次真实 Qwen 主链路验收与兼容修复

- **对应阶段：** 场景化能力优化计划 M4-D 的首个纵向验收切片 — 使用用户配置的真实 Qwen 模型验证 M4-B Evidence Summary/Query Proposal 与 M4-A Risk Narrative，同时确认失败时的确定性降级和 HITL 连续性。
- **文档原约定：** 真实模型调用必须通过显式开关启用，至少一次生成可追溯 Artifact；记录用途、尝试次数、延迟和可获得的 Token Usage，不记录完整 Prompt、原始响应或 Key。模型、网络、Schema 或引用失败时同一 Case 仍须通过确定性路径完成。
- **实际情况：** 首次隔离验收使用 `qwen3.7-plus`、Mock Research 与 Rules Verifier，M4-B 和 M4-A 两个逻辑用途均在约 186 秒、2 次应用级尝试后以 `MODEL_ERROR` 降级，但 Graph 正常进入 `WAITING_APPROVAL`。DNS、TCP 443 和不带凭据的 TLS 请求均正常，Base URL 为官方 OpenAI-compatible 路径。对照 DashScope 官方说明确认 `qwen3.7-plus` 默认开启混合思考，而 JSON Mode 需要关闭思考；同时 OpenAI SDK 默认内部重试与项目应用级重试叠加，使原定 30 秒超时扩展为约 186 秒。收口验证还发现 Chainlit 在测试收集期间会把用户 `.env` 加载到进程环境；原 Pytest 夹具只强制 Mock Search，未强制 `ANALYSIS_MODE=deterministic`，因此当用户配置为 `llm` 时默认回归存在误触发真实模型的风险。
- **修改内容：** `Settings` 和结构化模型网关新增可选 `ANALYSIS_LLM_ENABLE_THINKING`；只有显式配置时才通过 `extra_body` 发送 Provider 扩展参数，避免影响其他 OpenAI-compatible Provider。网关创建 OpenAI Client 时设置 `max_retries=0`，由 `ANALYSIS_LLM_MAX_RETRY` 唯一控制可审计尝试次数。Pytest 自动夹具新增 `deterministic`、Mock Search、禁用正文抓取、Rules Verifier 和空模型/搜索 Key 的离线覆盖；`scripts/verify.ps1` 在运行期间设置相同安全环境并在结束后恢复调用者原值。`.env.example`、README、优化计划和演示指南说明 DashScope Qwen JSON Mode 应设置 `false`；`.env` 未修改。新增配置解析、扩展参数透传和 SDK 重试关闭测试。
- **修改原因：** 结构化分析任务不需要长思维链；显式关闭 Qwen 思考模式既满足 JSON Mode 约束，也降低延迟和 Token 成本。消除 SDK 隐式重试后，配置的超时与尝试次数才与 Trace 和运维预期一致。
- **影响范围：** 未配置 `ANALYSIS_LLM_ENABLE_THINKING` 时现有 Provider 请求保持原样；显式设为 `false` 时发送 `enable_thinking=false`。应用级 `max_retry=1` 现在最多触发两次真实 HTTP 请求，不再叠加 SDK 的默认重试。普通 Pytest 和验证脚本不再受用户 `.env` 中联网模式影响；显式 live smoke 与正常应用运行不使用该测试夹具，行为不变。Research、Risk、HITL、Artifact Schema 和确定性降级路径不变；本轮未接入最终报告，M4-C 范围不变。
- **验证结果：** 修复后使用新 Thread `m4d-qwen-fixed-9dda9dc0a177` 再次执行相同隔离 Case。M4-B 在首次尝试 22.236 秒完成，记录输入 2184、输出 1158 Token；Evidence Summary 的 Verified ID 完整覆盖检查通过，三个 Query Proposal 均因确定性门禁拒绝且未改变三条规则 Query。M4-A 在首次尝试 10.569 秒完成，记录输入 1089、输出 559 Token；四条 Risk Explanation 的 Risk ID 和 Evidence 引用均在允许集合内。两个 Trace 均为 `LLM_CALL/SUCCESS`，任务停在 `WAITING_APPROVAL`，未发送正文、Rejected Evidence、Checkpoint、完整 Trace 或 Key。首次失败 Run 同时证明两个模型用途均可降级且主链路不中断。验证审计另发现两个既有 Chainlit UI Thread 在验收窗口内产生 6 个 Qwen Trace 事件、共记录 10 次 HTTP 尝试；成功事件合计记录输入 3146、输出 5332 Token，相关进程随后已结束。离线保护修复后完整门禁通过：Ruff lint/format、137 项 pytest、`pip check` 和独立 MCP stdio smoke 均成功，仅保留既有 `traceloop` Pydantic 弃用警告；最终门禁期间新增 Qwen 调用数为 0。M4-D 的 401/429、空输出、Schema 漂移等完整异常矩阵仍未实施，不将本切片表述为整个 M4-D 已完成。

---

## 2026-09-02 — M4-C1 受约束报告投影与 Unsupported Claim 二次门禁

- **对应阶段：** 场景化能力优化计划 M4-C 的第一实施切片 — 将 M4-A/B 已生成并通过首轮 Schema/引用校验的模型表达接入最终确定性报告。
- **文档原约定：** Executive Summary、Risk Explanation、Evidence Summary 和 Report Draft 使用独立 Prompt/Schema；重要陈述携带 Evidence ID；Unsupported Claim 检查拒绝未知 URL、Evidence ID、财务数字和确定性事实；最终报告只组装通过校验的表达，失败时回退确定性报告。
- **实际情况：** M4-A/B 已具备 Risk Narrative、Evidence Summary 和只供人工审核的 Query Proposal，但若立即增加第三个 Report Draft 模型用途，会扩大真实调用次数、输入面和验收成本。现阶段更优先验证“既有两次模型输出能否安全进入用户最终看到的报告”。
- **修改内容：** 将 M4-C 拆分为 C1/C2。C1 的 Report 节点不调用模型，新增 `report_expression_vN.json` 审计 Artifact，对 Risk ID 及所属 Evidence、已核验 Evidence/Claim/Fact、Evidence Gap、Query Plan/Intent、数字和 URL 执行第二次交叉校验；仅保留通过项，Query Proposal 固定标记为 `NOT_EXECUTED`。最终 Markdown 增加受约束 Risk/Evidence 表达、证据缺口、待审核查询建议和模型表达审计。可选模型 Artifact 缺失、损坏、状态非 `COMPLETE` 或引用不匹配时，报告仍使用确定性内容完成。Chainlit Artifact 列表与工作台详情读取新增 Report Expression。C2 保留为后续独立 Executive Summary/Report Draft 模型调用。
- **修改原因：** 先关闭“模型已经进入主链路但用户最终报告尚不可见”的核心缺口，同时复用现有真实 Qwen 验收产物并避免新增外部调用；把报告生成与模型可用性解耦，确保 HITL 后始终可完成。
- **影响范围：** Graph State、Report 节点、报告渲染、Artifact/Workbench 展示与 M4 测试文档；Financial、Research、Risk、路由、HITL、模型网关及 `.env` 均未改变。报告中的模型段落只来自 C1 接受列表，不改变确定性数值、事实状态、风险等级或人工意见。
- **验证结果：** 新增合法 Risk/Evidence/Gap/Query 投影、未知 URL 与数字、未知 Fact 拒绝、损坏源降级、报告泄漏防护和完整 HITL 主链路测试；Mock 主链路从启动到批准仍只调用 `evidence_summary_query_proposal` 与 `risk_narrative` 两个用途，Report 阶段零模型调用。专项 5 项测试通过；完整门禁通过 Ruff lint/format、142 项 pytest、`pip check` 与独立 MCP stdio smoke，仅保留既有 `traceloop` Pydantic 弃用警告。本轮未访问 Tavily、未调用真实 Qwen，也未读取或修改 `.env`。

---

## 2026-09-02 — M4-C2 Executive Summary 与 Report Draft 主链路接入

- **对应阶段：** 场景化能力优化计划 M4-C2 — 在 C1 已完成最终投影门禁的基础上，为 Report 节点增加独立的受约束报告表达调用。
- **文档原约定：** Executive Summary 与 Report Draft 使用独立 Prompt/Schema；每个重要陈述携带允许引用；模型不得改变财务数值、Verified Fact、Risk Level、路由或人工决定；模型失败时继续生成确定性报告。
- **实际情况：** M4-C1 已能安全消费 M4-A/B 表达，但没有独立 Report Draft 用途。Report 调用发生在风险任务人工批准之后，低风险任务则在直接进入 Report 时发生；若把人工意见交给模型，可能使模型改写审核事实，因此本阶段模型输入不包含 `human_comment` 或 `human_decision`。此前 M4-D 的真实 Qwen 验收只覆盖 M4-A/B，不能自动视为已覆盖本次新增用途。
- **修改内容：** 新增 Report Draft Prompt、Draft/Artifact/Section Schema 和 `build_report_draft` 服务。服务从 Company、Financial、Risk 与已核验 Fact 重建无 URL 的确定性来源索引，为索引计算 SHA-256 指纹，要求 Executive Summary 与 `financial_analysis/risk_analysis/evidence_assessment/limitations` 固定类别段落逐项引用允许 ID。输出版本化为 `report_draft_vN.json` 并记录受限 `purpose=report_draft` Trace。C1 投影器重新计算同一索引指纹，逐段复核引用、URL、数字与自动授信决定，只把通过项写入 `report_expression_vN.json` 和最终 Markdown。配置、模型、Schema、Prompt 或引用失败时 Report Draft 标记 `DEGRADED`，Report 节点不失败。State、Chainlit Artifact 列表、Workbench 详情和报告预览同步新增引用。
- **修改原因：** 让模型不仅生成局部风险/证据表达，也参与最终报告的摘要和段落组织，同时保持来源、数值、风险与人工审核边界由确定性代码控制。
- **影响范围：** `ANALYSIS_MODE=llm` 的完整高风险流程由两个模型用途增加为三个，新增调用仅在批准进入 Report 后执行；低风险任务在 Report 阶段也会执行一次。`deterministic` 模式不调用模型并写 `NOT_REQUESTED` Report Draft。Financial、Research、Risk、Graph Routing、HITL 决策和 `.env` 不变。
- **验证结果：** 已完成确定性零调用、合法引用、未知引用首轮降级、未知 URL/数字与自动授信决定二次剔除、模型不可用降级、完整 HITL 三用途调用、Artifact/Trace/报告投影和敏感字段不进入 Trace 的专项测试；相关 42 项回归及最终 11 项报告门禁测试通过。完整门禁通过 Ruff lint/format、148 项 pytest、`pip check` 与独立 MCP stdio smoke，仅保留既有 `traceloop` Pydantic 弃用警告。本轮未访问 Tavily、未调用真实 Qwen，也未读取或修改 `.env`。

---

## 2026-09-02 — M4-C2 比亚迪真实 Qwen/Tavily 纵向验收

- **对应阶段：** M4-D 对新增 M4-C2 Report Draft 的首次真实模型验收，同时复核真实 Tavily、正文抓取、Fact Verifier、HITL 和最终二次门禁。
- **文档原约定：** 使用用户配置的 Qwen 与公开 Case 生成可追溯 Artifact，记录用途、次数、耗时和 Token；外部搜索或模型失败时显式降级且主链路继续；无来源数字、事实或授信决定不得进入最终报告；真实 Key 不进入任何运行产物。
- **实际情况：** 新 Thread `m4c2-live-byd-20260902-a1`、Run `8327e217a5d4faac98f1` 使用 `qwen3.7-plus`、Tavily、HTTP/Auto 正文抓取和 LLM Fact Verifier。首次 Risk Narrative 1 次调用完成（8.235 秒，747/297 Token）；人工选择 Research 后，Tavily 对三个企业类别返回 15 条原始结果，确定性过滤保留 3 条，正文抓取 3/3 成功。一个网易候选的实际主题是易华录逾期，只因弱别名/类别命中被保留，证明候选门槛仍可能产生假阳性。Fact Verifier 没有读取 `ANALYSIS_LLM_ENABLE_THINKING=false`，对默认思考的 Qwen 三次均为 `INVALID_OUTPUT`；因此 Research 正确标记 `INCOMPLETE/UNVERIFIED`，零 Fact，并新增外部核验不完整风险。M4-B 在 1200 Token 输出预算下两次 `INVALID_OUTPUT`；对同一结构化输入将预算临时提高到 2400 后第二次成功，实际输出 1631 Token。第二轮 Risk Narrative 1 次调用完成（9.419 秒，809/483 Token）。批准后新增 Report Draft 首次尝试完成（19.267 秒，1313/866 Token），含执行摘要和四个固定段落。
- **修改内容：** 本轮验收没有修改运行代码或 `.env`；只将真实运行结果、配置出入和待修复项记录到优化计划与开发日志。验收子进程临时设置 `ANALYSIS_LLM_ENABLE_THINKING=false` 和 `FACT_VERIFIER=llm`，进程结束后恢复。
- **修改原因：** 保留真实失败证据，避免把 Report Draft 成功等同于整个互联网事实核查闭环成功；将 Provider 参数未统一、分用途输出预算不足和弱别名候选假阳性明确转入后续修复。
- **影响范围：** 仅新增 Git 忽略的 Run、Checkpoint、Trace、搜索/正文/核验缓存和文档记录。任务最终为 `COMPLETED/HIGH`，`external_research_incomplete=true`；`report_draft_v1.json` 为 `COMPLETE`，`report_expression_v1.json` 为 `PARTIAL`。二次门禁拒绝执行摘要与财务段落中的百分比换算和未引用阈值推导，接受其余三个 Report Draft 段落和三条 Risk Narrative；报告包含调查不完整披露与人工意见，未出现自动授信决定。
- **验证结果：** Trace 共 34 条，含 4 条主链路 `LLM_CALL`：两条 Risk Narrative 成功、一条 M4-B 降级和一条 Report Draft 成功；Research Tool 一次成功，记录 `candidates=3/fetched=3/verify_failed=3`。按运行期间的实际尝试计，主链路结构化模型请求 5 次、Fact Verifier 请求 3 次，另有 M4-B 2400 Token 对照请求 2 次；Tavily 对三个类别各返回最多 5 条结果。扫描本 Run、Trace 与报告，模型 Key 和 Tavily Key 均未泄漏。M4-C2 的真实调用、审计与安全降级门禁通过，但 Fact Verifier 兼容性、M4-B 默认输出预算和候选假阳性仍未通过，不将本轮表述为整个真实事实核查链路完成。

---

## 2026-09-02 — M4 真实验收问题修复与 Snapshot/Qwen 复验

- **对应阶段：** M4-D 真实模型兼容性修复，以及 M2.1-A/C 候选过滤和事实核验的验收收口。
- **文档原约定：** Qwen JSON Mode 显式关闭思考；模型输出必须符合受约束 Schema，错误时按配置重试并安全降级；M4-B 生成完整 Evidence Summary/Query Proposal；无关主体不得进入正文核验、事实、风险或报告。
- **实际情况：** 首次验收中 Fact Verifier 没有继承主分析的 non-thinking 配置且 OpenAI Client 保留 SDK 默认重试；即使关闭思考，诊断调用仍得到语法有效但 Schema 非法的 JSON：Qwen 将 `subject_match` 返回为布尔值，并自造 `INSUFFICIENT_EVIDENCE` 关系枚举。M4-B 的实际成功输出达到 1631 Token，超过共用的 1200-token 上限。网易易华录文章因正文摘要偶然出现“比亚迪”而成为 C 级候选。
- **修改内容：** Fact Verifier 新增可选 `FACT_VERIFIER_ENABLE_THINKING`，未配置时继承 `ANALYSIS_LLM_ENABLE_THINKING`；发送完整 `VerifierDecision` JSON Schema 并在系统约束中列明固定枚举；关闭 SDK 隐式重试，对非法结构执行受限应用级重试；升级 Prompt 版本避免复用失败缓存。结构化模型网关支持单次输出预算覆盖，M4-B 使用独立的 `ANALYSIS_LLM_RESEARCH_MAX_OUTPUT_TOKENS=2400`。公司查询的 C 级来源增加标题主体门槛和 `SUBJECT_TITLE_MISMATCH`，新增易华录固定负向 Snapshot。`.env.example`、README、演示指南、优化计划和验证脚本同步；`.env` 未修改。
- **修改原因：** Provider 的“JSON 对象”只保证语法，不保证项目枚举和类型；必须把 Schema 明示并保留 Pydantic 边界校验。分用途预算避免为全部模型调用无差别扩大输出。C 级弱来源缺少足够可信度，标题主体门槛可以在付费/高成本正文和模型步骤前消除已复现的误召回。
- **影响范围：** 事实核验缓存键随 Prompt 版本变化，旧失败缓存不会命中新逻辑；Fact Verifier 的真实尝试次数现在与 `FACT_VERIFIER_MAX_RETRY` 一致。标题门槛仅适用于公司查询的 C 级来源，不改变 A/B 来源、行业查询、可信离线夹具和已核验事实聚合规则。M4-B 增加独立配置但 Risk Narrative/Report Draft 默认上限不变。
- **验证结果：** 单文档真实 Qwen smoke 返回 `EXACT/INSUFFICIENT`、置信度 0.95、`execution_status=COMPLETE`、`error_code=None`。随后以新 Thread `m4-fix-e2e-20260902-220338`、Run `859b9fbc6fb6990197ef` 和独立核验缓存执行 Snapshot + 真实 Qwen 全链路：15 条原始结果中 1 条候选、14 条拒绝；候选核验首轮完成且 0 失败，Research 为 `COMPLETE/UNVERIFIED`；易华录条目以 `SUBJECT_TITLE_MISMATCH` 拒绝且没有 Verification。M4-B 首轮完成（1875/1011 Token），两次 Risk Narrative 与 Report Draft 均完成，最终任务 `COMPLETED/HIGH`。Report Expression 为 `PARTIAL`，仅因 `financial_analysis` 中模型自行转换数字被 `UNSUPPORTED_NUMBER` 正确拒绝；Risk/Evidence 段落保留。扫描 Run、Trace 和独立核验缓存，模型 Key 与 Tavily Key 均未出现。最终离线全量门禁通过：Ruff lint/format、152 项 pytest、`pip check` 与独立 MCP stdio smoke 全部成功，仅保留第三方 Traceloop 的既有 Pydantic 弃用警告。

---

## 2026-09-05 — M5-A 固定离线业务 Eval 第一切片

- **对应阶段：** 场景化能力优化计划 M5.2 Agent Eval 的第一实施切片，为后续多 Case Eval 和审计包建立版本化、可重复执行的离线基线。
- **文档原约定：** 建立 5–10 个公开或脱敏 Case，评价财务准确性、异常召回、引用、恢复、工具可靠性和人工审核完整性；默认测试不得访问外部服务，结果应可比较、可审计。
- **实际情况：** 原计划只有目标指标和完成定义，没有 Suite Manifest、隔离执行、评分结果或 CLI 契约。现有比亚迪 Case 已有人工财务、异常和风险预期，且已有华谊兄弟、易华录两条真实误召回负向夹具，适合作为首个纵向 Eval；引用有效性和真实模型文风仍需要更多已核验公开事实样本，不能在本切片伪造统计意义。
- **修改内容：** 先在优化计划补充 M5-A 第一切片设计和门禁；新增严格的 `EvalSuiteManifest`、逐项 `EvalCheck`、Case/Suite Result Schema，版本化 Suite `byd_baseline_v1`、隔离 Runner 和 `python -m app.eval_cli run`。Runner 将 Case Source 复制到唯一 `eval-results/` 运行目录，使用独立 Checkpoint/Trace，通过正式 `start_task → WAITING_APPROVAL → resume approve → COMPLETED` 链路生成 Artifact 和报告；显式强制 deterministic、Mock Search、禁用正文抓取、Rules Verifier 和人工基准中的阈值，不读取 `.env` 文件或用户联网模式。结果记录 Suite 指纹、16 项 PASS/FAIL、聚合指标、Run 引用和 `external_call_count=0`。新增路径穿越防护、唯一目录、基线漂移失败和密钥哨兵不进入结果测试；`.gitignore` 忽略 `eval-results/`。
- **修改原因：** 将分散在单元测试中的人工预期升级为用户可直接运行的业务验收入口，同时复用真实 Durable Runtime，避免 Eval 与产品链路产生两套实现。首版只对有人工答案的指标给出 1.0/0.0，不把无 Verified Fact 的 Case 虚假计入 Citation Coverage。
- **影响范围：** 新增 `app/evals/`、`app.eval_cli`、`evals/suites/`、Eval 测试和文档；不改变主 Graph、State、Artifact Schema、搜索/模型 Provider 或 `.env`。运行输出只写 Git 忽略的 `eval-results/`，不同执行不覆盖。5–10 Case 扩充、真实 Snapshot/Qwen 评分、审计 ZIP 和运行指标增强仍属于后续 M5 切片。
- **验证结果：** 默认 Suite 实际运行返回 `PASS`，16/16 检查通过，全部聚合指标为 1.0，`external_call_count=0`；输出位于唯一 `eval-results/byd_baseline_v1-<execution>/eval_result.json`。针对性 10 项测试通过，包括人为修改营收增长预期后 Suite 稳定返回 `FAIL`、项目路径穿越被拒绝、环境中的模型/搜索 Key 哨兵未进入结果，以及既有比亚迪基线和跨进程 HITL 回归。完整门禁通过：Ruff lint/format、154 项 pytest、`pip check` 与独立 MCP stdio smoke 全部成功，仅保留第三方 Traceloop 的既有 Pydantic 弃用警告；门禁期间外部模型和搜索调用数为 0。

---

## 2026-09-05 — M5-B 单次任务审计包第一实施切片

- **对应阶段：** 场景化能力优化计划 M5.3 审计包导出和 M5.4 运行指标的第一实施切片。
- **文档原约定：** 已完成任务可导出报告、Evidence、来源、Artifact、Trace 摘要、运行指标与总清单组成的 ZIP；审计包能够独立说明输入、来源和执行路径，不泄漏 Key、原始 Trace、Checkpoint 或正文 Snapshot。
- **实际情况：** 原计划只列出目标目录树，没有定义任务定位、旧运行兼容、覆盖策略、成员白名单、大小限制、路径/符号链接边界、哈希校验、原子写入或 CLI 错误语义。实现期首次安全扫描还发现 Windows 绝对路径正则会把 `https://` 尾部的 `s:/` 误判为盘符路径。Codex 沙箱为 pytest/`TemporaryDirectory` 创建的目录具有异常 Windows ACL，业务测试需按项目验证约定在沙箱外使用仓库 `.test-tmp`；普通用户环境和现有验证脚本不受该沙箱特性影响。
- **修改内容：** 先在优化计划补充 M5-B 第一切片边界和门禁；新增严格的 Audit Manifest/Result Schema、`app.audit` 导出服务和 `python -m app.audit_cli export`。服务只接受安全 Thread ID、`COMPLETED` 状态和新式 Run ID，通过现有 Checkpoint 定位任务；输出包含转义 HTML/Markdown 报告、Case 来源清单、受限 Evidence 投影、版本化 JSON Artifact、Artifact 哈希清单、聚合 Trace、运行指标和总清单。Checkpoint、原始 Trace、搜索/正文/核验 Snapshot、故障状态和 `.env` 不进入包。全部路径、文件名、符号链接、文件数和大小受限；扫描当前配置凭据、常见凭据字段和 Windows 绝对路径；在临时目录组装后重新打开 ZIP 校验成员集合、CRC、大小和 SHA-256，再原子落盘。同名 ZIP 不覆盖，默认目录 `audit-exports/` 已加入 Git 忽略。Windows 路径正则增加前导字母边界，保留 `C:\\...` 拦截且不误伤 HTTP(S) URL。运行模式只从历史 Run Artifact 推导；复查真实包时发现若使用导出时 Settings 会把历史 Snapshot/Qwen 误记为当前 Tavily/Rules，因此已移除该做法，无法由现有 Artifact 证明的正文抓取 Provider 明确记录为 `NOT_RECORDED`。README 和演示指南新增导出、解包与人工核对命令；`.env` 未修改。
- **修改原因：** 审计导出本身必须是一个安全、可复查的产品边界，不能简单压缩整个 Run 目录；明确成员投影和不可变写入可避免把运行凭据、本机路径、缓存正文或可变状态交付给审核方。
- **影响范围：** 新增 `app/audit/`、`app.audit_cli`、审计导出测试、文档和 `.gitignore`；不改变 Graph、业务 State、Financial/Research/Risk/Report 计算、HITL、Provider 配置或 `.env`。导出不修改业务 State 或 Run Artifact，但会通过正式状态查询新增一条受限 `STATUS_QUERY` Trace；不执行 Tavily、正文抓取、Fact Verifier 或 Qwen。旧 Case 级运行和未完成任务在首版显式拒绝。
- **验证结果：** 定向测试 2 项通过，覆盖包成员与双清单哈希、CLI、执行模式与运行指标、敏感 Key 哨兵、本机路径、重复输出、危险 Thread ID、未完成任务、损坏 Artifact 和失败不残留 ZIP。复用真实已完成比亚迪任务 `m4-fix-e2e-20260902-220338` 成功导出 21 个成员、13 个 Artifact 的最终审计包；独立重新打开后 CRC 和 Manifest 全部哈希通过，ZIP SHA-256 为 `9e369f91daeddf1b7c6d0f6b5a6ea4ce65a691ab73b8be9d3cbc7aa7edeea573`。包内从历史 Artifact 正确识别 `analysis_mode=llm`、模型 `qwen3.7-plus`、`search_sources=[snapshot]`、正文/Verifier 执行完成和 `degradation_used=true`，同时将未持久化的正文 Provider 标为 `NOT_RECORDED`。完整离线门禁通过：Ruff lint/format、156 项 pytest、`pip check` 与独立 MCP stdio smoke 全部成功，仅保留第三方 Traceloop 的既有 Pydantic 弃用警告；门禁和导出期间新增模型/搜索调用数为 0。PDF 报告、多 Case 统计 Eval、Provider 精确 credits 和工作台内一键导出仍是后续增强，不将本切片表述为整个 M5 已完成。

---

## 2026-09-05 — M5-A2 上汽集团第二套公开 Case 候选材料

- **对应阶段：** 场景化能力优化计划 M5-A2 — 在比亚迪单 Case Eval 之外，引入第二家 A 股汽车上市公司，验证通用 Case Schema、来源血缘和确定性业务链路。
- **文档原约定：** 使用 2023—2025 连续年度公开报告，优先交易所来源，统一财务字段和 CNY_1000 内部口径；先完成材料、字段血缘和 Case Validation，再由人工审核候选 Financial/Anomaly/Risk 基线，未经审核不得固化为 Eval 真值。
- **实际情况：** 上交所可获得 2025 年年度报告，上市公司投资者关系官网可获得 2002、2023、2024 年报告及现行公司章程；2002 年报告用于核验更名前同一法人主体的首次注册日期。源表单位为人民币元，报告期包含汽车金融子公司，经营现金流受制造业务与金融业务规模共同影响；因此现金流下降不能静默解释为制造业务单一恶化。一次正式任务探查未先覆盖用户 `.env` 中的 `ANALYSIS_MODE=llm`，Risk Narrative 尝试了两次 Qwen 请求并以 `MODEL_ERROR/DEGRADED` 结束，没有成功 Token 统计；确定性风险、路由和待审核状态未受影响。后续离线演示命令已显式覆盖 Provider，避免重复该情况。
- **修改内容：** 在优化计划补充 M5-A2 实施状态；新增 `data/case_saic_600104/source/` 的公司资料、三年财务数据、业务边界和 v2 Source Manifest；2023—2025 年收入、归母净利润与经营现金流使用 2025 年报告最新比较表，2024—2025 年资产负债表使用同一报告，2023 年资产负债表使用 2024 年报告比较数。每组字段记录来源、PDF 页、表名、合并口径、源单位和除以 1,000 的换算规则；`.gitignore` 仅对白名单 Case 开放该目录；增加上汽材料预检和上交所来源边界测试；演示指南增加强制离线的审核流程。
- **修改原因：** 上汽与比亚迪同属汽车行业但股权、合营企业、汽车金融和现金流结构差异明显，适合检验现有方法是否只拟合首个 Case；保留人工审核门禁可避免把规则引擎当前输出直接等同于业务事实。
- **影响范围：** 新增公开结构化 Case、测试和文档，不修改 Graph、State、Artifact Schema、风险阈值、Provider 配置或 `.env`。仓库不保存完整年报 PDF；视觉核验使用的临时 PDF 与页面图像已删除。上汽尚未加入固定 Eval，吉利汽车港股对照 Case 尚未开始结构化。
- **验证结果：** 交易所 2025 年报告第 5、65、66 页、公司 2024 年报告第 68、69 页、现行章程第 3 页和 2002 年报告第 5 页已渲染并逐项核对；Case CLI 返回 `VALID`、零 Error/Warning/Confirmation。确定性预览得到营业收入增长率 `[-0.1544, 0.052238]`、流动比率 `[1.131829, 1.176221, 1.19691]`、资产负债率 `[0.659358, 0.63774, 0.623721]`，候选风险为 `MEDIUM/cashflow`；正式候选任务停在 `WAITING_APPROVAL`，未代替用户执行批准。专项 Case 测试 9 项通过；完整门禁通过 Ruff lint/format、157 项 pytest、`pip check` 与独立 MCP stdio smoke，仅保留第三方 Traceloop 的既有 Pydantic 弃用警告；完整门禁未新增模型或搜索调用。

---

## 2026-09-06 — M5-A2 上汽集团并入固定业务 Eval

- **对应阶段：** 场景化能力优化计划 M5-A2 — 将已审核的上汽集团候选材料固化为第二套业务真值，并验证同一 Eval 方法能够覆盖深交所与上交所 Case。
- **文档原约定：** 上汽结构化材料、字段血缘和确定性候选基线经人工审核后，方可生成 `expected_*` 真值并加入固定离线 Eval；正式 Durable Runtime 必须零外部调用完成，增加交易所来源兼容性检查，且既有比亚迪 Suite 和全量回归继续通过。
- **实际情况：** 既有 Eval Manifest 只描述业务 Artifact 预期，尚无市场与主来源 Host 契约；若直接复制比亚迪 Suite，无法证明上交所材料通过了通用 Case Validation。上汽当前也没有经人工确认、可重复的无关证据负样本，不能为了与比亚迪检查数一致而虚构样本。定向 pytest 首次仍因本机系统 Temp 目录 ACL 在 Fixture Setup 阶段失败，业务断言未执行；按项目既有约定改用仓库 `.test-tmp` 隔离目录后正常通过。
- **修改内容：** 固化上汽 `expected_financial`、`expected_anomalies`、`expected_risk` 和人工验收清单；新增默认双 Case Suite `auto_manufacturers_v1`，同时保留 `byd_baseline_v1` 单 Case Suite。Eval Case 契约增加成对配置的可选 `expected_market` 与 `expected_primary_source_host`；Runner 在启用该契约时通过正式 Case Validation 检查材料有效性，并核对 Source Manifest 的市场与至少一个主来源 Host。CLI 默认切换到双 Case Suite；增加默认参数、双 Case 运行、来源契约和上汽结果测试，并同步 README、演示指南与优化计划。
- **修改原因：** 让第二套 Case 真正进入用户可执行的统一验收入口，并用最小、可审计的来源契约证明 A 股不同交易所来源可复用相同 Schema；可选字段保证旧 Suite 的结果和检查数不发生隐式变化。
- **影响范围：** 默认 `python -m app.eval_cli run` 从比亚迪 16 项检查变为比亚迪加上汽共 32 项；原比亚迪 Suite 仍可显式运行且保持 16 项。没有修改 Graph、State、Artifact Schema、财务/风险规则、Provider 或 `.env`，没有发起搜索或模型调用。上汽 Eval 只使用结构化公开材料与离线 Provider，吉利汽车港股对照 Case 尚未开始。
- **验证结果：** 默认双 Case Eval 实际返回 `PASS`、32/32 检查通过、全部聚合指标为 1.0、`external_call_count=0`；上汽结果覆盖 Financial、Anomaly、Risk、HITL/Resume、Report 和 `source.case_contract`。定向 Ruff 通过，业务 Eval 与 Case Validation 共 13 项 pytest 通过。完整离线门禁通过 Ruff lint/format、159 项 pytest、`pip check` 与独立 MCP stdio smoke；仅保留第三方 Traceloop 的既有 Pydantic 弃用警告，验证过程未新增模型或搜索调用。

---

## 2026-09-06 — M3-D1 Chainlit Agent 流程总览

- **对应阶段：** 场景化能力优化计划 M3-D1 — 在现有对话式工作台上增加不复制业务状态的 Agent 流程可视化第一切片。
- **文档原约定：** 本地工作台展示执行节点、当前/下一节点、人工审核和报告状态，UI 与 CLI 共用 Durable Runtime；工作台只展示已有可信状态，不承担修补后端业务语义。经本轮补充，M3-D 分为流程总览、公开执行步骤、业务图表和可选实时事件桥接四部分，D1 不承诺节点运行中的实时动画。
- **实际情况：** 现有 `_node_states` 已能从 State、Artifact 引用、Interrupt 和失败字段推导六节点状态，但仅以 Markdown 长文本展示。Chainlit 2.11.1 支持内嵌 JSX Custom Element，可复用该确定性投影；同步 Runtime 通过后台线程运行且操作结束后才返回 Payload，因此首版只能在启动、恢复、审核或刷新完成后生成快照。浏览器控制台另有 `.chainlit/config.toml` 既有文件上传 `accept=["*/*"]` 的无效 MIME 警告，与新增组件无关，本切片未扩展范围修复。
- **修改内容：** 在优化计划中新增 M3-D1～D4 的范围和门禁；新增 `public/elements/AgentFlow.jsx`，以卡片、状态图标和进度条展示材料解析、财务分析、补充调查、风险分析、人工审核和报告生成。`app.chainlit_app` 增加受限 `_flow_view`，把既有中文状态映射为 `pending/running/done/waiting/skipped/failed`，仅投影 Case、Thread、Run、任务状态、节点状态、下一节点、风险等级和进度；每次 `_send_payload` 将该组件与现有报告文件一同附加。README、演示指南和 UI 测试同步。
- **修改原因：** 让非开发用户无需从 Markdown 表格和 Trace 中自行还原流程，同时保持 Checkpoint/State 为唯一真值，不在浏览器维护第二份工作流状态；严格 Props 白名单避免把完整 Artifact、Trace、Prompt、正文或密钥传入自定义前端。
- **影响范围：** 只修改 Chainlit 展示投影、自定义 JSX、测试和文档；Graph、Runtime、Checkpoint、State、Artifact Schema、HITL Action、CLI、Provider 和 `.env` 均未改变。工作流卡片是操作完成后的状态快照，真正实时更新仍属于 M3-D4。
- **验证结果：** 定向 Ruff 通过，Chainlit UI 与 Workbench 共 15 项 pytest 通过；浏览器以离线确定性模式启动上汽 Case，卡片正确显示 `WAITING_APPROVAL/MEDIUM`、4/6、Research 跳过、Approval 等待和 Report 待执行，刷新后新快照与同一 Thread/Run 一致，前端无 JSX 编译错误。完整离线门禁通过 Ruff lint/format、161 项 pytest、`pip check` 与独立 MCP stdio smoke；仅保留第三方 Traceloop 的既有 Pydantic 弃用警告，开发与验收未调用真实模型或搜索服务。
