# Credra Agent 项目协作约定

## Git 提交流程

- 在每个关键开发节点完成后，准备一次独立、聚焦的 Git 提交。
- 关键节点包括但不限于：技术 Spike 通过、核心业务纵向链路完成、动态路由完成、MCP 调查链路完成、HITL/Resume 完成、可靠性与回归测试完成、最终展示材料完成。
- 执行任何 `git commit` 前，必须先向用户展示本次拟提交的文件、变更摘要和建议提交信息，并等待用户明确人工审核与批准。
- 未获得用户明确批准时，不得创建提交，也不得代替用户判断审核已经完成。
- 每次提交应保持范围清晰，不把无关修改混入关键节点提交。

## 开发文档与变更记录

- 代码开发必须严格遵守 `docs/` 目录中的需求分析、技术设计方案和开发计划；实现范围、优先级、架构边界及验收标准均以这些文档为依据。
- 开始一个开发阶段前，应先核对对应文档；完成后，应按照文档规定的阶段门禁和验收标准验证结果。
- 如果实际依赖版本、框架行为、运行环境或验证结果与开发文档存在出入，或者实现需要作适应性修改，不得静默偏离文档。
- 所有上述出入与适应性修改都必须另外记录在 `docs/开发日志.md` 中，并至少注明：对应阶段、文档原约定、实际情况、修改内容、修改原因、影响范围和验证结果。
- 开发日志用于记录实施事实和设计偏差，不直接覆盖原始需求或开发计划；如需正式变更基线，应先向用户说明并经人工审核后再修改相应文档。
- 实施与验证记录按大的开发阶段维护，每个 `V2-x` 阶段最多保留一份对应记录；单个工作包或步骤不得新增独立实施文档，其进展追加到阶段记录和 `docs/开发日志.md`。
- 新增文档前先检查现有需求、架构、开发计划、阶段实施记录和使用说明能否承载；可以承载时必须更新现有文档，避免重复描述。

## 代码与文件组织

- 生产代码、测试、CLI、Schema、运行标识和 Prompt 版本使用稳定的功能或领域名称，不使用 `P20`、`P23` 等开发计划编号命名。计划编号只用于开发计划、阶段实施记录和开发日志的追踪。
- 新文件或新模块必须对应清晰且可独立说明的职责；现有模块能够自然承载时优先扩展现有实现，不为单个开发步骤创建临时模块。
- 测试文件按被测功能或模块命名，并与生产模块保持可识别的对应关系；测试名称描述行为和预期结果，不描述开发顺序。
- Pytest 测试按职责放入 `tests/application/`、`tests/analysis/`、`tests/agentic/`、`tests/entry/`、`tests/financial/`、`tests/research/`、`tests/observability/` 或 `tests/runtime/`；测试辅助进程放入 `tests/support/`，需要显式运行的在线或协议冒烟程序放入 `tests/smoke/`，共享样本保留在 `tests/fixtures/`。除 `conftest.py` 和包初始化文件外，不在 `tests/` 根目录新增测试或辅助脚本。
- 公共命名一旦持久化或形成兼容接口，修改时必须说明迁移和恢复影响；尚未发布的步骤编号命名应在提交前改为功能命名。

## 环境配置保护

- `.env` 由用户维护。除非用户在当前请求中再次明确授权，否则后续开发不得修改这个文件；可以通过修改 `.env.example` 并告知用户来通知更新。
- 读取环境配置时不得输出、记录或提交任何真实 API Key；配置验证只检查字段是否存在或使用测试占位值。

## 项目命名

- 产品名称统一为 `Credra Agent`。
- Python 包或模块语义名统一为 `credra_agent`。
